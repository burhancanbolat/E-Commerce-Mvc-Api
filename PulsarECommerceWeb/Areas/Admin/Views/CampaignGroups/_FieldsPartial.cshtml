@model CampaignGroup

<div class="mb-3">
    <label class="form-label" asp-for="Name"></label>
    <input class="form-control" asp-for="Name">
    <span asp-validation-for="Name"></span>
</div>
<div class="mb-3">
    @if (ViewContext.RouteData.Values["Action"].ToString() == "Edit")
    {
        <div class="mb-4">
            <img src="@Model.BackgroundImage" width="200" height="200" class="img-thumbnail" />
            <div class="alert alert-info mt-2">
                Değiştirmek istiyorsanız doldurunuz!
            </div>
        </div>
    }
    <label for="formFile" class="form-label" asp-for="BackgroundImage"></label>
    <input class="form-control" type="file" accept="image/*" id="BackgroundImageFile" name="BackgroundImageFile">
    <div class="alert alert-info mt-2">
        <ul class="list-unstyled mb-0">
            <li>Yüklenen görseller otomatik olarak 1920&times;1080 piksel boyutuna getirilir</li>
            <li>En iyi görünüm için 1920&times;1080 ve katları  boyutunda görseller yükleyiniz</li>
        </ul>
    </div>
</div>
<div class="mb-3">
    <div class="form-check form-switch">
        <input class="form-check-input" type="checkbox" asp-for="Enabled">
        <label class="form-check-label" asp-for="Enabled"></label>
    </div>
</div>
